import { Image } from "react-native";
import React from "react";
import { icons } from "@/constants";

type Props = {};

const ArrowRight = (props: Props) => {
  return <Image source={icons.arrowRight} />;
};

export default ArrowRight;
